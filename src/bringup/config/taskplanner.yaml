mock_vlm_node:
  ros__parameters:
    period_sec: 2.0

phase_estimator:
  ros__parameters: {}

or_digital_twin:
  ros__parameters: {}

surgeon_actor:
  ros__parameters:
    decision_period_sec: 0.25
    min_tool_use_sec: 3.0

bt_decision_bridge:
  ros__parameters:
    target_node_name: /tree_executor
    mirror_period_sec: 0.2

tree_executor:
  ros__parameters:
    # auto_apms interprets this as the interval in seconds: 0.025 s = 40 Hz.
    tick_rate: 0.025
    groot2_port: 0
    state_change_logger: true
