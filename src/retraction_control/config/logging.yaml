schema: retraction_control.runtime.v1
storage:
  # Launch may override this with another absolute directory.
  data_directory: /tmp/retraction-control
  ledger_filename: command_ledger.sqlite3
  session_directory_name: teaching_sessions
  shadow_trace_directory_name: shadow_traces
  atomic_fsync: true
publish:
  status_period_sec: 0.5
  diagnostics_period_sec: 1.0
