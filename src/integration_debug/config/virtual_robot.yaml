schema: taskplanner.action_emulator.v1
profile_id: integration_debug_virtual_robot_v1
routes:
  tool_handover:
    available: true
    default:
      outcome: success
      # Keep the virtual handover observable in the operating-room UI.  The
      # previous 50 ms completion was shorter than a normal browser refresh and
      # made the full action progression effectively invisible.
      duration_sec: 4.0
      reason_code: virtual_debug_completed_no_robot_motion
  retraction_command:
    available: true
    default:
      outcome: success
      duration_sec: 0.0
      reason_code: virtual_admission_only_no_robot_motion
